defaults:
  - model: adalayers_bert
  - optimization: adam
  - tokenizer_pretrained: bert_mlm
  - dataset: cola
  - hydra: common
  - _self_

optimization:
  max_epochs: 16

wandb:
  name: ${cat:"test", ${dataset.name}, ${model.name}}
