defaults:
  - model: adalayers_roberta_base
  - optimization: adam
  - tokenizer_pretrained: bert_mlm
  - dataset: imdb
  - hydra: common
  - _self_

optimization:
  max_epochs: 32

wandb:
  name: ${cat:"final", ${dataset.name}, ${model.name}}
